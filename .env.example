# EmailJS Configuration
# Get these values from your EmailJS dashboard (https://emailjs.com)
VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# GitHub API Token (Optional but Recommended)
# Without a token: 60 requests/hour
# With a token: 5000 requests/hour
# 
# How to get a token:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "Portfolio Website"
# 4. Select scope: public_repo (read-only access to public repos)
# 5. Copy the token and paste it below
# 
# Note: The portfolio will work without a token, but with reduced rate limits
VITE_GITHUB_TOKEN=your_github_token_here
